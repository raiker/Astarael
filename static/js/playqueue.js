//Module handles all the management of the play queue
//Exposes some methods to allow songs to be added, etc
define([], function () {
	var playqueue = [];
	var currentindex = -1;

	return {
		append: function (trackid) {
		}
	};
});